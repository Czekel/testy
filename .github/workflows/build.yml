name: Build mod

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pobranie kodu
        uses: actions/checkout@v4

      - name: Instalacja Javy 21
        uses: actions/setup-java@v4
        with:
          distribution: 'microsoft'
          java-version: 21

      - name: Ustawienie Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10"

      - name: Budowanie moda
        run: gradle build --no-daemon

      - name: Wgranie gotowego .jar jako plik do pobrania
        uses: actions/upload-artifact@v4
        with:
          name: shadow-katana-mod-jar
          path: build/libs/*.jar
